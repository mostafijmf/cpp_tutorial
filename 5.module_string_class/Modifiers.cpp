#include <bits/stdc++.h>
using namespace std;
int main()
{
    string a = "HelloWorld";
    string b = "World";
    // a += b; // append another string.
    // a.append(b); // append another string.
    // a.push_back('A'); // add character to the last of the string.
    // a.pop_back(); // remove the last character of the string.
    // a.erase(3, 2); // erase characters from the string. it takes 2 parameters: string index, string char count
    // a.replace(4, 0, "agb"); // replace a portion of the string.
    // a.insert(5, "AMI"); // insert a portion to a specific position.
    cout << a << endl;

    return 0;
}
